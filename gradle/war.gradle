apply plugin: "war"

bootWar {
    mainClassName = "com.darya.jhipster.SecondAttemptApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
